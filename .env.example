# Frontend build-time config (Vite).
# Leave blank for a same-origin deploy (frontend and API on the same domain).
# Set to your API's full URL if the backend is on a different origin,
# e.g. VITE_API_BASE=https://api.tinymedia.example
VITE_API_BASE=
